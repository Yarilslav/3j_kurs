# Frontend

React + Vite demo interface for Tea House Asahi.

## Local run
```powershell
npm install
npm run dev
```

## Docker run
Use root `docker compose up --build`.

## Routes
- `/`
- `/auth`
- `/catalog`
- `/catalog/:id`
- `/booking`
- `/admin`
